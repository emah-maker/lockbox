<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This job stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->

# FRAIM Job: fully-delegate

## Intent
Take a high-level goal end to end: understand the delegation path, create the right dependency graph, run independent jobs in parallel, coach sub-agents through verification loops until every output meets the quality bar, then hand a synthesized DRAFT to the human for final approval.

## Outcome
- A clear delegation path or a documented clarity escalation when the path cannot be understood safely
- A dependency-aware execution graph recorded before any sub-agent work begins
- Verified outputs from every sub-agent, with iteration history per job
- A recorded manager verdict on each sub-agent's own review surface, so the review lives where the work is
- One evidence file at `docs/evidence/{anchor}-fully-delegate-evidence.md`: who delegated to whom, what each sub-agent produced (with links to each sub-agent's evidence file and pull request), iteration history, risk areas, and the human approval checklist
- One review surface for the human: a pull request for repository-backed work, or the deliverable set in the thread when no issue tracker is configured
- Reusable learnings captured for future hardening

## Steps
1. Phase 1: `listen`
2. Phase 2: `understand-delegation-path`
3. Phase 3: `create-delegation-graph`
4. Phase 4: `execute`
5. Phase 5: `document-learnings`
6. Phase 6: `submit`
7. Phase 7: `address-feedback`
8. Phase 8: `retrospective`

---

> [!IMPORTANT]
> **For AI Agents:** Do NOT attempt to execute this job based on the Intent/Outcome above.
> This stub is for discoverability only. The actual job has multiple phases with
> detailed steps, validation criteria, and required skills.
>
> To get full phase-by-phase instructions, call:
> `get_fraim_job({ job: "fully-delegate" })`
>
> Then follow all phases using `seekMentoring` at each phase transition.
