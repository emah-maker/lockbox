<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: android-project-setup

## Skill Input
- Project name and Android application ID (reverse-DNS, e.g., `ai.fraimworks.mobile`)
- Target SDK version and minimum SDK version (Play Store policy currently requires `targetSdkVersion` ≥ 34 for new submissions)
- Third-party libraries needed (Compose, Coroutines, Retrofit, etc.)
- Toolchain decision (native Kotlin/Gradle, React Native + EAS, Flutter)
- Signing strategy (Play App Signing — recommended — or self-managed upload key)

## Skill Output
- Gradle project structure with version catalogs
- Kotlin + Compose + Hilt scaffolding
- Build variants (debug, staging, release) with proper applicationId suffixing
- Play App Signing configured: upload keystore generated locally; upload certificate registered with Play Console
- AAB output path documented for the submission job to consume

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/android/android-project-setup.md" })`
