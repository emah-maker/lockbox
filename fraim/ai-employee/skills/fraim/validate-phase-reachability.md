<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: validate-phase-reachability

## Skill Input
- `parentPhases`: the routing table from the baseline job's frontmatter (`phases` object).
- `childPhases`: the routing entries defined in the override's frontmatter (`phases` object). Empty for a full teach-mode job.
- `initialPhase`: the entry-point phase ID (from the parent, or from the child for teach mode).

## Skill Output
- A reachability verdict: pass or a list of orphaned phase IDs with their disposition.
- Explicit confirmation from the author for every orphaned phase before the file is written or accepted.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/fraim/validate-phase-reachability.md" })`
