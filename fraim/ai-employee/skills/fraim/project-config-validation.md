<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: project-config-validation

## Skill Input
- Expected existence of generated files (e.g., `docs/architecture/architecture.md`).
- Expected key-value attributes inside `fraim/config.json`.

## Skill Output
Confirmation that the workflow's configured artifacts physically exist on disk and that `fraim/config.json` correctly references them.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/fraim/project-config-validation.md" })`
