<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: crm-connect

## Skill Input
- The CRM provider configured for this project, read from `fraim/config.json` under `crm.provider`. Supported values: `salesforce`, `customereq`, `attio`. If not set, ask the user.

## Skill Output
- A live, authenticated connection to the configured CRM.
- The full CRM adapter context (tools, signals, write-back approach) for the connected provider, so downstream job phases can invoke the right operations without referencing a specific CRM.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/crm/crm-connect.md" })`
