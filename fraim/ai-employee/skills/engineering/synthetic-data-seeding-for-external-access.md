<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: synthetic-data-seeding-for-external-access

## Skill Input
- A target SaaS / multi-tenant system that requires sign-in to demonstrate value
- A target user identity (synthetic, e.g., `apple_demo@<domain>`) that must show populated data
- A list of UI surfaces the external party (App Reviewer, prospect, recorded-walkthrough viewer) will visit
- Admin credentials or DB write access to the target system

## Skill Output
- A documented admin endpoint or script that idempotently seeds the target identity with realistic-looking data covering every UI surface
- Confirmation (visual or via API) that every surface is populated when the synthetic identity signs in
- Documented credentials handed off to the consumer (App Review notes, prospect demo doc, etc.)

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/engineering/synthetic-data-seeding-for-external-access.md" })`
