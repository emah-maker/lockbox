<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: choose-sharing-backend

## Skill Input
- `configKey`: the top-level key to write in `~/.fraim/config.json` (e.g. `organization`, `managerStorage`, `projectStorage`)
- `layerLabel`: human-readable name for the layer shown in conversation (e.g. "organization's shared context", "manager agreements")
- `personalBearing`: `true` when this layer holds personal data — enables the git right-to-erasure warning

## Skill Output
- The caller's `~/.fraim/config.json` is updated with the chosen backend under `configKey`, or left unchanged if the manager chose "this machine only".

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/learning/choose-sharing-backend.md" })`
