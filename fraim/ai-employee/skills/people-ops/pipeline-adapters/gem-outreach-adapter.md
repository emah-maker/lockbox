<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: gem-outreach-adapter

## Skill Input
- `apiKey`: GEM API key, read from env var `GEM_API_KEY`. Never hard-coded.
- `reqIds[]` (optional): Req or job IDs to scope the pull. Omit for all active sequences.
- `reportingWindow`: `{ days: number, label: string }`.
- `trendWindow` (optional): `{ days: number, label: string }` — prior window for trend comparison.

## Skill Output
- `sequenceMetrics[]`: One entry per active outreach sequence:
  ```
  {
    sequenceId, sequenceName, reqId, reqTitle,
    sends, bounces, opens, replies, positiveReplies,
    trendWindow: { sends, bounces, opens, replies, positiveReplies } | null
  }
  ```
- `sourceLabel`: `"Gem (API)"`.
- `warnings[]`: API errors, partial data, zero-result notices.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/people-ops/pipeline-adapters/gem-outreach-adapter.md" })`
