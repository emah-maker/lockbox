<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: browser-outreach-adapter

## Skill Input
- `provider`: Outreach or sourcing tool name (e.g. `"gem"`, `"outreach"`). Used to determine which pages to navigate.
- `reqIds[]` (optional): Req or job IDs to scope the pull.
- `reportingWindow`: `{ days: number, label: string }`.

## Skill Output
- `sequenceMetrics[]`: One entry per sequence extracted from the UI:
  ```
  {
    sequenceId, sequenceName, reqId, reqTitle,
    sends, bounces, opens, replies, positiveReplies,
    trendWindow: null
  }
  ```
  `trendWindow` is always `null` in browser mode — prior-window trend data is not reliably extractable from the UI.
- `sourceLabel`: `"{provider} (browser)"`.
- `warnings[]`: Always includes a fragility warning; may include login-required or missing-column warnings.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/people-ops/pipeline-adapters/browser-outreach-adapter.md" })`
