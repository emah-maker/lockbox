<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: gem-api-adapter

## Skill Input
- `apiKey`: GEM API key, read from env var `GEM_API_KEY`. Never hard-coded.
- `role` (optional): Role or project name to scope the pull.

## Skill Output
- `candidates[]`: `{ name, role, currentStage, lastActivityDate, nextStep, owner }` for all active sourcing candidates.
- `stages[]`: Ordered sourcing stage list with `{ stageName, entryCount, exitCount }`.
- `sourceLabel`: `"GEM"`.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/people-ops/pipeline-adapters/gem-api-adapter.md" })`
