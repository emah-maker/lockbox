<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: ats-adapter

## Skill Input
- The `integrations.recruiting` block from `fraim/config.json`. Shape:
  ```json
  {
    "ats": { "provider": "greenhouse|lever|ashby|none", "pipeline_source": "api|browser|csv|manual" },
    "leadgen": { "provider": "gem|linkedin-recruiter|apollo|outreach|none", "pipeline_source": "api|browser|csv|manual" },
    "staleness_days": { "sourcing": 14, "applied": 5, "screen": 5, "interview": 3, "offer": 1 }
  }
  ```

## Skill Output
- The provider-specific ATS adapter loaded into context with its full data-loading steps available to downstream phases.
- The provider-specific LeadGen adapter loaded into context (if configured).
- `stalenessThresholds`: Resolved per-stage thresholds for this run.
- `sourceLabels`: `{ ats: "<tool display name>", leadgen: "<tool display name or null>" }`.
- `warnings[]`: Credential issues or config gaps.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/people-ops/pipeline-adapters/ats-adapter.md" })`
