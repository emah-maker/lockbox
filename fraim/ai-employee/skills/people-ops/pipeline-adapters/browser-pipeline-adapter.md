<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: browser-pipeline-adapter

## Skill Input
- `provider`: ATS or LeadGen tool name (e.g., `"greenhouse"`, `"lever"`, `"gem"`). Used to determine which pages to navigate.
- `layer`: `"ats"` or `"leadgen"`. Determines which pipeline view to open.
- `role` (optional): Role or job name to filter results.

## Skill Output
- `candidates[]`: `{ name, role, currentStage, lastActivityDate, nextStep, owner }`.
- `stages[]`: Stage list with counts where extractable from the UI.
- `sourceLabel`: `"{provider} (browser)"`.
- `warnings[]`: Always includes a fragility warning.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/people-ops/pipeline-adapters/browser-pipeline-adapter.md" })`
