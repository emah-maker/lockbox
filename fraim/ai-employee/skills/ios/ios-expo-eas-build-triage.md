<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: ios-expo-eas-build-triage

## Skill Input
- Expo app directory in a monorepo (Ashley: `ashley-rn/`).
- Target build profiles (`preview`, `development`, etc.).
- Latest EAS build errors/log links when failures occur.

## Skill Output
- Identified root cause for EAS iOS build failures with evidence.
- Validated fix before re-queue (via inspected archive when relevant).
- Re-queued builds and status summary with build IDs.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/ios/ios-expo-eas-build-triage.md" })`
