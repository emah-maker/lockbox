<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: support-ingest-adapter

## Skill Input
- The resolved support queue provider: `{{proxy.config.automation.support.queue.provider | INFORM | "Use fixture or manual ticket context unless a live support queue provider is configured."}}`.
- The raw event or ticket payload to normalize, supplied by the calling job phase.

## Skill Output
- A normalized FRAIM support ticket object conforming to the standard ticket abstraction, ready for the `classify-request` phase.
- When the configured provider has no ingest adapter (e.g. `servicenow`, `fixture`), the ticket is already normalized by the connect phase — pass it through unchanged.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/support/support-ingest-adapter.md" })`
