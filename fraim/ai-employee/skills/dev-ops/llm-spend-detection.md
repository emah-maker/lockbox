<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: llm-spend-detection

## Skill Input
- `working_dir` — the user's primary repo path (and optionally additional repos to scan).
- `home_dir` — the user's home directory (`$HOME` or `$env:USERPROFILE`), used to inspect agent CLI configs.

## Skill Output
A structured detection report listing every `(agent_or_app, llm_provider, evidence_path)` triple found, ranked by likely volume. The report is narrow and concrete — no guesses about providers the user *might* use.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/dev-ops/llm-spend-detection.md" })`
