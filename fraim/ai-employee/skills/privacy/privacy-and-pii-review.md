<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: privacy-and-pii-review

## Skill Input
- `reviewScope`: One of `{repo, directory, branch, pr, diff}`.
- `surfaceAreaPaths`: The files or directories under review.
- `diff` (optional): The PR / branch diff when the review is incremental.
- `projectGotchas` (optional): Project-specific patterns such as known PII fields, logging conventions, and subprocessor list location.
- `subprocessorsListPath` (optional): Path to the subprocessor or DPA registry, so new third-party SDK additions can be checked for coverage.

## Skill Output
- `findings`: Per-finding array with `owasp_code` in `PRIV01..PRIV05`.
- `coverage`: One row per privacy category with status `{Pass, Fail, N/A, Error}`.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/privacy/privacy-and-pii-review.md" })`
