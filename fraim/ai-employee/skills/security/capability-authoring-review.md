<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: capability-authoring-review

## Skill Input
- `diff`: The PR/branch diff to review. The skill only inspects files matching the capability-authoring surface (see path globs below); other files are ignored.
- `projectGotchas` (optional): Project-specific adjustments — trusted authors whose edits can relax specific checks, skill paths that intentionally carry adversarial content for testing, etc.

## Skill Output
- `findings`: Array of findings per the `security-review` schema, each with `owasp_code: "LLM01"` (prompt-injection class) or `owasp_code: "CAP-AUTH"` when the issue is a capability-governance one rather than injection.
- `coverage`: Matrix row per sub-check with status `{Pass, Fail, N/A, Error}`.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/security/capability-authoring-review.md" })`
