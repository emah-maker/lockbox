<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: owasp-llm-top-10-review

## Skill Input
- `reviewScope`: One of `{repo, directory, branch, pr, diff}`.
- `surfaceAreaPaths`: The LLM-surface files or directories under review.
- `diff` (optional): The PR / branch diff when the review is incremental or when change-aware evidence should be prioritized.
- `projectGotchas` (optional): Project-specific LLM patterns such as prompt delimiter conventions, known-safe template locations, or approved tool allowlists.

## Skill Output
- `findings`: Per-finding array with `owasp_code` in `LLM01..LLM10`.
- `coverage`: One row per OWASP LLM Top 10 category.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/security/owasp-llm-top-10-review.md" })`
