<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: sf-soql-query

## Skill Input
- `conn`: A jsforce Connection with a valid `accessToken` (from `sf-auth` skill).
- `objectType`: Salesforce sObject API name (e.g. `Opportunity`, `Case`, `Account`).
- `fields`: List of field API names to retrieve. If omitted, introspect schema to determine queryable fields.
- `whereClause`: Optional SOQL WHERE expression (e.g. `StageName = 'Prospecting' AND Amount > 500000`).
- `limit`: Optional row limit (default: 200; use Bulk API 2.0 for >50,000 rows).
- `orderBy`: Optional ORDER BY clause.

## Skill Output
- An array of sObject records matching the query, each containing the requested fields.
- A schema summary if introspection was run (field names, types, whether field is queryable/updateable).

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/salesforce/sf-soql-query.md" })`
