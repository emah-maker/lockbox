<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: salesforce

## Skill Input
- Salesforce JWT Bearer credentials: `SF_INSTANCE_URL`, `SF_CONSUMER_KEY`, `SF_PRIVATE_KEY_PATH` (or `SF_PRIVATE_KEY`), `SF_USERNAME`. Read from environment or vault.
- Optional: `SF_API_VERSION` (default `66.0`).

## Skill Output
- An authenticated jsforce `Connection` ready for SOQL, CRUD, Bulk API, Metadata API, and Agentforce calls.
- Guidance for all adapter operations available to downstream job phases.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/salesforce/salesforce.md" })`
