<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: m365-account-unlock

## Skill Input
- Target user principal name (UPN).
- Optional `enableAccount` boolean (default `true`). When `false`, the skill disables the account instead (for offboarding playbooks).

## Skill Output
- `upn`, `previousState` (`enabled` or `disabled`), `newState`, and `alreadyInState` when no change was needed.

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/microsoft/m365-account-unlock.md" })`
