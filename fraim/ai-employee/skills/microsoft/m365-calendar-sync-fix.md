<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: m365-calendar-sync-fix

## Skill Input
- Target user principal name (UPN).
- `symptom`: one of `missing-events`, `duplicate-events`, `sharing-broken`, `mobile-not-syncing`, `general` (default: `general`).

## Skill Output
- `upn`, `calendarSettings` (current mailbox time zone, working hours, auto-accept settings), `sharingPermissions` (list of calendar sharing grants for the user's primary calendar), `diagnosisSteps` (array of checks and findings), `actionsApplied` (array of remediation steps taken), `requiresManualStep` (boolean).

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/microsoft/m365-calendar-sync-fix.md" })`
