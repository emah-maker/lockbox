<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This skill stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->
# FRAIM Skill: m365-license-assign

## Skill Input
- Target user principal name (UPN).
- `skuPartNumber` (human-readable, e.g., `ENTERPRISEPREMIUM`) OR `skuId` (GUID). The script resolves part number to GUID via GET `/subscribedSkus`.

## Skill Output
- `upn`, `skuId`, `skuPartNumber`, `action` (`assigned`), `availableUnits` (remaining after action).

---

> [!IMPORTANT]
> **For AI Agents:** This is a discoverability stub for the skill.
> All execution details must be fetched from MCP before use.
> To retrieve the complete skill instructions, call:
> `get_fraim_file({ path: "skills/microsoft/m365-license-assign.md" })`
