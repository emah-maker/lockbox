<!-- FRAIM_SYNC_MANAGED_CONTENT -->
> [!IMPORTANT]
> This job stub is synced from FRAIM and will be overwritten on the next `fraim sync`.
> Do not edit this file.
<!-- FRAIM_DISCOVERY_STUB -->

# FRAIM Job: author-project-rules

## Intent
Create or refine the canonical repo rule file that every connected agent must read before doing work.

## Outcome
- `fraim/personalized-employee/rules/project_rules.md` exists or is updated in local FRAIM context.
- The file captures repo-wide operating rules only, not one-off learnings.
- Rules are concise enough to inject into `fraim_connect`, `get_fraim_job`, and `seekMentoring` without overwhelming the agent.

## Steps
1. Phase 1: `inventory-project-expectations`
2. Phase 2: `draft-project-rules`
3. Phase 3: `validate-project-rules`
4. Phase 4: `submit`

---

> [!IMPORTANT]
> **For AI Agents:** Do NOT attempt to execute this job based on the Intent/Outcome above.
> This stub is for discoverability only. The actual job has multiple phases with
> detailed steps, validation criteria, and required skills.
>
> To get full phase-by-phase instructions, call:
> `get_fraim_job({ job: "author-project-rules" })`
>
> Then follow all phases using `seekMentoring` at each phase transition.
